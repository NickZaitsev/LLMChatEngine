
services:
  ai-girlfriend-bot:
    build: .
    container_name: ai-girlfriend-bot
    restart: unless-stopped
    env_file: .env
    environment:
      - DATABASE_URL=postgresql+asyncpg://ai_bot:${DB_PASSWORD:-ai_bot_pass}@postgres:5432/ai_bot
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - backup_network
    extra_hosts:
      - "host-machine:host-gateway"
  postgres:
    image: pgvector/pgvector:pg15
    container_name: ai-girlfriend-postgres
    restart: unless-stopped
    environment:
      - POSTGRES_DB=ai_bot
      - POSTGRES_USER=ai_bot
      - POSTGRES_PASSWORD=${DB_PASSWORD:-ai_bot_pass}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-pgvector.sql:/docker-entrypoint-initdb.d/init-pgvector.sql
      - backups:/backups
    ports:
      - "5432:5432"
    networks:
      - backup_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ai_bot -d ai_bot"]
      interval: 5s
      timeout: 3s
      retries: 3

  postgres-backup:
    build: ./backup
    container_name: ai-girlfriend-postgres-backup
    restart: unless-stopped
    environment:
      - DB_PASSWORD=${DB_PASSWORD:-ai_bot_pass}
    volumes:
      - backups:/backups
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - backup_network

  redis:
    image: redis:alpine
    container_name: ai-girlfriend-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    networks:
      - backup_network

  celery-worker:
    build: .
    container_name: ai-girlfriend-celery-worker
    restart: unless-stopped
    command: celery -A proactive_messaging worker --loglevel=info --statedb=/app/celery_worker_state.db -Q proactive_messaging
    env_file: .env
    environment:
      - REDIS_URL=redis://redis:6379/0
      - DATABASE_URL=postgresql+asyncpg://ai_bot:${DB_PASSWORD:-ai_bot_pass}@postgres:5432/ai_bot
    depends_on:
      - redis
      - postgres
    volumes:
      - .:/app
      - celery_worker_state:/app/celery_worker_state
    networks:
      - backup_network
    extra_hosts:
      - "host-machine:host-gateway"

  celery-beat:
    build: .
    container_name: ai-girlfriend-celery-beat
    restart: unless-stopped
    command: celery -A proactive_messaging beat --loglevel=info --scheduler celery.beat.PersistentScheduler -s /app/celerybeat-schedule/schedule
    env_file: .env
    environment:
      - REDIS_URL=redis://redis:6379/0
      - DATABASE_URL=postgresql+asyncpg://ai_bot:${DB_PASSWORD:-ai_bot_pass}@postgres:5432/ai_bot
    depends_on:
      - redis
      - postgres
    volumes:
      - .:/app
      - celery_beat_schedule:/app/celerybeat-schedule
    networks:
      - backup_network
    extra_hosts:
      - "host-machine:host-gateway"

  celery-memory:
    build: .
    container_name: ai-girlfriend-celery-memory
    restart: unless-stopped
    command: celery -A memory.tasks:celery_app worker --loglevel=info -Q memory_tasks
    env_file: .env
    environment:
      - PYTHONPATH=/app
      - REDIS_URL=redis://redis:6379/0
      - DATABASE_URL=postgresql+asyncpg://ai_bot:${DB_PASSWORD:-ai_bot_pass}@postgres:5432/ai_bot
    depends_on:
      - redis
      - postgres
    volumes:
      - .:/app
    networks:
      - backup_network
    extra_hosts:
      - "host-machine:host-gateway"

volumes:
  postgres_data:
  backups:
  redis_data:
  celery_beat_schedule:
  celery_worker_state:

networks:
  backup_network:
    driver: bridge